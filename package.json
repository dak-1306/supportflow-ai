{
  "name": "supportflow-ai",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "pnpm --filter @supportflow/shared-types build && turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.0.0",
    "prettier": "^3.0.0",
    "eslint": "^8.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
